/** The first Bikram Sambat year this package supports (inclusive). */
export const MIN_BS_YEAR = 1979;

/** The last Bikram Sambat year this package supports (inclusive). */
export const MAX_BS_YEAR = 2100;

/** The number of months in a Bikram Sambat year. */
export const MONTHS_PER_YEAR = 12;
